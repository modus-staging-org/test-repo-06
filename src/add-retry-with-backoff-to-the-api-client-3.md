# Add retry with backoff to the API client

Bad input reached storage and failed there, leaving partial state. Validation now happens before any write.

Change #3 of 3 on branch `pr/20260811-121032-3-add-retry-with-backoff-to-the-api-client`.
